{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 4000",
    "build": "next build",
    "build:static": "next build && npx export -o '../public'",
    "action:build": "npm i && npx next build && npx next export -o '../public'",
    "start": "next start"
  },
  "dependencies": {
    "next": "^10.0.0",
    "phpmorphy-locutus": "^0.1.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.32.13"
  },
  "devDependencies": {
    "@types/react": "^17.0.5",
    "typescript": "^4.2.4"
  }
}
